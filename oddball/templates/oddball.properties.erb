default.bins = default-bins.txt
repository.type = cloud
config.container = <%= config_container %>
config.path = <%= config_path %>
cloud.type = aws-s3
cloud.identity = <%= cloud_identity %>
cloud.credential = <%= cloud_credential %>
kinesis.application.name = <%= kinesis_application_name %>
kinesis.stream = <%= kinesis_stream %>
consumers = <%= consumers %>
security = default
user.manager.db.host = <%= user_manager_db_host %>
security.administrator.role = <%= security_administrator_role %>