db.host = <%= @db_host %>
db.name = <%= db_name %>

config.repository.type = cloud
resources.repository.type = cloud
cloud.type = aws-s3
cloud.identity = <%= cloud_identity %>
cloud.credential = <%= cloud_credential %>
resources.container = <%= resources_container %>
config.container = <%= config_container %>
config.path = <%= config_path %>